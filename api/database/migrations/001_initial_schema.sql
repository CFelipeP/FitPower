-- Migration 001: Initial schema (applied via schema.sql manually)
-- This file exists to fill the gap in migration numbering.
-- All tables were created by schema.sql before tracking began.
SELECT 1;
